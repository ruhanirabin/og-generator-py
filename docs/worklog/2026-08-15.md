# 2026-08-15 — public README refresh

## Completed

- Rewrote the README around the personal One Small Fix origin story and linked
  the published Substack post.
- Reorganized usage documentation for a public audience, including source
  installation, quick-start examples, all current CLI options, all five themes,
  logo behavior, output naming and formats, batch generation, determinism, and
  development checks.
- Removed repository access-status language and recorded the public-facing
  documentation stance in project memory.
- Added a root `VERSION` source of truth, configured Hatchling to read it, and
  exposed the resolved package metadata through `ogimg.__version__` and
  `ogimg --version`.
- Added a Keep a Changelog-style `CHANGELOG.md`, version-aware module headers
  across source, test, and example Python files, and README links and guidance.
- Added a short README disclosure explaining the role of AI tooling and the
  agent-oriented project memory, tests, worklogs, and `AGENTS.md` conventions.
- Corrected the One Small Fix publication link to
  `https://onesmallfix.substack.com/` and added concise cross-platform install
  troubleshooting for Python, pipx, Git, PATH setup, the GitHub source-archive
  fallback, and automatic Pillow installation.

## Verification

- Cross-checked the documented command surface against `src/ogimg/cli.py`,
  `src/ogimg/render.py`, `src/ogimg/templates/__init__.py`, and the tests.
- `git diff --check`: passed.
- `python -m pytest`: 26 tests passed in an isolated Python 3.14.6 environment.
- `python -m ruff check .`: passed.
- `python -m ruff format --check .`: passed for 17 Python files.
- `python -m build`: produced `ogimg-0.1.0.dev0.tar.gz` and
  `ogimg-0.1.0.dev0-py3-none-any.whl`.
- The installed `ogimg --version` command and built wheel metadata both
  reported `0.1.0.dev0`, matching the root `VERSION` file.

## Related

- [README](../../README.md)
- [Project memory](../project-memory.md)
- [Published backstory](https://mind.ruhanirabin.com/projects/og-images-without-making-it-a-whole-design-flip-flop)
