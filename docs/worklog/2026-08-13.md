# 2026-08-13 — initial project scaffold

## Completed

- Reviewed the structure, repository instructions, durable project memory, and
  dated worklog conventions in `hugo-rabin-blog`.
- Added concise repository instructions tailored to a cross-platform Python
  image-generation CLI.
- Recorded the initial architecture boundaries, cross-platform constraints,
  implementation scope, and open decisions.
- Added a public-facing README that clearly marks the project as not yet
  implemented.
- Added source, built-in-template, test, and example directory markers without
  introducing application code or dependencies.
- Implemented the first vertical slice with Hatchling metadata, a `src/` package,
  an installable `ogimg` entry point, one `og` preset, and one
  `midnight-violet` procedural gradient theme.
- Added glyph-measured title partitioning and font-size fitting with explicit
  overflow errors and deterministic PNG encoding.
- Bundled Noto Sans Bold with its SIL Open Font License 1.1 text and resolved it
  with `importlib.resources`.
- Added unit, rendering, determinism, and CLI tests and synchronized the README
  with implemented behavior.
- Adopted the MIT License and recorded Ruhani Rabin with
  `https://www.ruhanirabin.com` as the public author identity in package
  metadata and documentation.
- Created and uploaded the project to the private GitHub repository
  `ruhanirabin/og-generator-py`; added repository and issue URLs to package
  metadata and documented access-controlled Git and pipx installation.
- Added optional PNG or WebP logo compositing with deterministic proportional
  resizing; the logo remains at a fixed top anchor and the title starts after a
  fixed gap, while proprietary/user logo assets remain external.
- Extended logo placement with `auto`, `top-left`, `top-center`, and `top-right`.
  Automatic placement crops transparent margins, centers visible wordmarks at
  least 2:1, and places compact marks at the top left. Corner placement leaves
  centered title geometry unchanged.

## Verification

- `python -m pytest`: 10 tests passed on Linux with Python 3.14.6 and Pillow
  12.3.0.
- `python -m ruff check .`: passed.
- `python -m ruff format --check .`: passed.
- Generated `output/vertical-slice.png`; Pillow confirmed PNG, RGB, and
  1200×630 dimensions.
- Visually inspected the generated image for gradient, centering, wrapping,
  clipping, and readability.
- Generated and visually inspected
  `/home/rabin/Downloads/article-with-logo.png` using an external transparent
  WebP logo; verified logo sharpness, transparency, spacing, title balance,
  and absence of overlap or clipping. The source logo was not copied into the
  repository.
- Visually compared automatic top-left and explicit top-right renders using the
  external square Just One Fix logo. Wide-wordmark centering and visible-bounds
  classification are covered by focused rendering tests.
- `python -m build`: created the source distribution and wheel successfully.
- Rebuilt after adding project ownership metadata; wheel metadata reports
  `Author: Ruhani Rabin`, `License-Expression: MIT`, and the public homepage,
  and includes both the project MIT license and bundled font OFL text.
- Inspected the wheel contents and confirmed that the package includes Noto
  Sans Bold and `OFL.txt`.
- Installed the wheel into a clean temporary environment; the installed entry
  point generated a valid PNG with the expected mode and dimensions.
- Confirmed the local `main` branch tracks `origin/main` at initial commit
  `e6314cd`, with the expected GitHub remote; `git diff --check` passed for the
  documentation and metadata update.
- macOS and Windows were not tested; compatibility is designed, not verified.

## Sources

- Python Packaging User Guide: `pyproject.toml`, Hatchling, `requires-python`,
  and `[project.scripts]` guidance.
- Pillow documentation: `ImageDraw.textbbox` for TrueType glyph measurement.
- Noto fonts repository and bundled `OFL.txt`: font origin and redistribution
  terms.

## Related

- [Project memory](../project-memory.md)
- [Repository instructions](../../AGENTS.md)
- [README](../../README.md)
